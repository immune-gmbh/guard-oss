immune Guard On Premise package
===============================

`service/`: Kubernetes resources for running the server.

`tool/`: `ops` tool for generating siging keys. Needs GNU Make and Go 1.17+.
